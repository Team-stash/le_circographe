guest_user_role:
  user_id: <%= User.find_by(email_address: 'guest@example.com').id %>
  role_id: <%= Role.find_by(name: 'guest').id %>

admin_user_role:
  user_id: <%= User.find_by(email_address: 'admin@example.com').id %>
  role_id: <%= Role.find_by(name: 'admin').id %>
