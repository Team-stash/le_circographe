event_1:
  title: Test Event 1
  date: <%= 2.days.from_now %>
  creator_id: <%= User.find_by(email_address: 'admin@example.com')&.id || User.first&.id %>

event_2:
  title: Test Event 2
  date: <%= 1.week.from_now %>
  creator_id: <%= User.find_by(email_address: 'guest@example.com')&.id || User.second&.id %>
