<div class="admin-dashboard">
<%= form_with url: admin_notepad_url, method: :put do |form| %>
  <%= text_area_tag "notepad", @notepad%>
  <%= submit_tag "Mettre à jour", class: "purple-button" %>
<% end %>
<div>
